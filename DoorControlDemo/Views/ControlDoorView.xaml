<Window x:Class="DoorControlDemo.Views.ControlDoorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoorControlDemo.Views"
        mc:Ignorable="d"
        Title="ControlDoorView" Height="450" Width="800">
    <Grid>
        

        <!-- Door Operation Buttons (to be implemented) -->
        <StackPanel Grid.Column="1" Margin="20">
            <Button Content="Open Door" Command="{Binding OpenDoorCommand}" Margin="0 5" />
            <Button Content="Close Door" Command="{Binding CloseDoorCommand}" Margin="0 5" />
            <Button Content="Stay Open" Command="{Binding StayOpenCommand}" Margin="0 5" />
            <Button Content="Stay Close" Command="{Binding StayCloseCommand}" Margin="0 5" />
        </StackPanel>
    </Grid>
</Window>

